// next.config.ts
/** @type {import('next').NextConfig} */
const nextConfig = {
  // No custom webpack config, no experimental flags.
  // Next.js will use its default compiler and settings.
};

export default nextConfig;